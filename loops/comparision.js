let x = "5";
console.log(x == 5);


let age = 16;
 if (age < 18)
console.log( text="Too young to buy alcohol");



{let age = 20;
let voteable = (age < 18) ? "Too young":"Old enough";
 console.log( voteable + " to vote.");
}

{let age = 20;
if (isNaN(age)) {
  voteable = "Input is not a number";
} else {
  voteable = (age < 18) ? "Too young" : "Old enough";
  console.log( voteable + " to vote.");//can we give values in output
}}